import { renderCLOSED_DA} from "./components/CLOSED_DA.js";

addEventListener(("DOMContentLoaded"),(e)=> {
    console.log("test")
    renderCLOSED_DA();
});


// Render your React component instead
//ReactDOM.render(<CLOSED_DA />,document.querySelector("#app"));
